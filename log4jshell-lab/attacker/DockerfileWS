FROM ubuntu:22.04

#Update package list and install ssh
RUN apt-get update && \
    apt-get install -y \
    netcat \
    default-jre

ADD /basicJar /basicJar

EXPOSE 443

#Keep container running
ENTRYPOINT ["tail", "-f", "/dev/null"]
